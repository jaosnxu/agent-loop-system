# Test Standard

- Every acceptance criterion must have at least one check.
- Browser tests must record pass/fail counts and report path.
- Permission checks must verify hidden or blocked controls.
- Russian layout checks must detect overflow, clipping, or mojibake.
- Failed tests return to the build role with root cause and next checks.
